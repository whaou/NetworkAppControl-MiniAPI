#!/usr/bin/make -f

#export DH_VERBOSE = 1
export PYBUILD_NAME = miniapi
export PYBUILD_DISABLE_python2=1

override_dh_builddeb:
	dh_builddeb -- -Zgzip	# for old debian version that do not support zstd compression

%:
	dh $@ --with python3 --buildsystem=pybuild
